{
  "name": "goalconnect-monorepo",
  "private": true,
  "version": "0.1.0",
  "description": "GoalConnect monorepo for component-based group project",
  "workspaces": [
    "packages/*",
    "systems/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces --if-present",
    "build": "npm run build -w @goalconnect/ui-components && npm run build -w @goalconnect/utils && npm run build -w @goalconnect/feature-x && npm run build -w @goalconnect/feature-y && npm run build --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "devDependencies": {
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "eslint": "^10.2.0",
    "prettier": "^3.8.2",
    "tsup": "^8.5.1",
    "typescript": "^6.0.2"
  },
  "dependencies": {
    "react": "^19.2.5",
    "react-dom": "^19.2.5"
  }
}
